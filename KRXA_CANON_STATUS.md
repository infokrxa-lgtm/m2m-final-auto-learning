# KRXA LOCAL BUILD v3 CANON

Status: CANONICAL LOCAL ROOT

This build merges:
- v1 base directory structure
- v2 execution engine

Use this folder as the only active local working root.

## Run

```bat
python run_krxa.py status
python run_krxa.py run-m2m
python run_krxa.py recover
python run_krxa.py reset
```

## Rule

- Keep v1 as archive only.
- Keep v2 as archive only.
- Work only inside v3_CANON.
